language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"

sudo: false

# command to install dependencies
install:
  - "pip install ."
  - "pip install -r requirements.txt"

# command to run tests
script: nosetests tests/test_alert.py tests/test_alertdoc.py

